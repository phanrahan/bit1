from setuptools import setup
import sys

setup(
    name='bit1',
    version='0.1',
    description='Bit1',
    packages=[
        "bit1",
    ],
    install_requires=[
    ],
    python_requires='>=3.6'
)
